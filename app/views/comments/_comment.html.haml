.row.comment
  .col-md-12.content
    %p.name
      %strong= comment.name
    %p.date
      Commented
      = time_ago_in_words(comment.created_at)
      ago
    %p.body= comment.body
  - if user_signed_in?
    .buttons
      = link_to "Delete", [comment.post, comment], method: :delete, class: "button", data: { confirm: "Are you sure?" }, class: "btn my-btn"